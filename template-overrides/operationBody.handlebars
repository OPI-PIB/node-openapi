{{{bodyMethodTsComments}}}{{methodName}}({{>operationParameters}}): Observable<{{{resultType}}}> {
    return this.{{responseMethodName}}(params, reportProgress, context).pipe(
      map((r: {{@root.responseClass}}<{{{resultType}}}>): {{{resultType}}} => r.body)
    );
  }
